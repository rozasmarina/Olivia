<div class="container">

  <div class="d-flex justify-content-center align-items-center">
    <h1>Rankings</h1>

  </div>

  <ul class="list-group list-group-flush">
    <% @places.each do |place| %>
    <li class="list-group-item d-flex justify-content-between align-items-center list-place">
      <%= link_to place.name, place_path(place) %>
      <span class="badge badge-roxa badge-pill"> 3 </span>
    </li>
    <% end %>
  </ul>

</div>


  </div>
</div>

<% if policy(@places).new? %>
  <p><%= link_to 'Adicionar um novo local', new_place_path %></p>
<% end %>
